/var/log/keenetic_*.log {
    daily
    # compress
    # delaycompress
    notifempty
    missingok
    rotate 15
    dateext
    create 0666 {{ ansible_user }} {{ ansible_user }}
    postrotate
        /usr/bin/systemctl -s HUP kill rsyslog.service >/dev/null 2>&1 || true
    endscript
}
